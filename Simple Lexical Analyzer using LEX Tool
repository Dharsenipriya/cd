%{
#include <stdio.h>
%}

%%
[a-zA-Z_][a-zA-Z0-9_]*    { printf("Identifier: %s\n", yytext); }
[0-9]+                    { printf("Number: %s\n", yytext); }
"/*"([^*]|\*+[^*/])*\*+"/" { printf("Comment: %s\n", yytext); }
"//".*                    { printf("Comment: %s\n", yytext); }
[+\-*/=<>!&|]             { printf("Operator: %s\n", yytext); }
[\n\t ]+                  ; /* ignore spaces */
.                         { printf("Other: %s\n", yytext); }
%%

int main() {
    yylex();
    return 0;
}
